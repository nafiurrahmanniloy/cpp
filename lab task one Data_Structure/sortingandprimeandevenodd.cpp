#include<iostream>
using namespace std;
int main()
{
    int n,a[100],i,j,temp,flag;
    cout<<"enter the size of the array: ";
    cin>>n;
    cout<<"enter all the elements of the array : ";
    for(i=0;i<n;i++)
    {
        cin>>a[i];
    }
   /* cout<<"now sorting big to small: ";
    int pass;
    for(pass=0;pass<n-1;pass++)
    {
        flag=0;
        for(j=0;j<(n-pass-1);j++)
        {
            if(a[j]<a[j+1]){
               temp= a[j];
                a[j]=a[j+1];
                a[j+1]=temp;
            }
            flag=1;
        }
        if(flag==0)
        break;
        }

        for(i=0;i<n;i++)
        {
            cout<<a[i]<<" ";
        }*/
   /*     cout<<"finding the prime number : ";
        for(i=0;i<n;i++)
        {
            flag=0;
            for(j=2;j<a[i];j++){
                if(a[i]%j==0)
                    flag++;
            }
        if(flag==0)
        {
            cout<<a[i]<<" " ;
        }
        }

      */

   /*cout<<"finding even odd number : ";
   for(i=0;i<n;i++)
   {

       flag =0;
      if(a[i]/2==0)
      {
                flag=1;
      cout<<a[i]<<" " ;
      }
      else if(a[i]/2!=0)
      {
          flag=2;
          cout<<a[i]<<" " ;

      }

   }*/














    }

